# Lib.Db v2.2 최적화 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Lib.Db v2.1.0 → v2.2.0: Critical 10건 + Major 29건 + Minor 14건 + 신규 7건 = 총 60건의 버그 수정, 메모리/안전성 개선, 중복 제거, 성능 최적화, 한글 주석 완비, MARS 정책 통합

**Architecture:** 6개 Track으로 분리하여 순차 실행. Track 1(TvpGen)과 Track 2(메모리/안전)는 병렬 가능. Track 3(중복/아키텍처)→Track 4(성능)→Track 6(MARS)는 순차. Track 5(주석)는 최종.

**Tech Stack:** .NET 10 / C# 14, Roslyn Incremental Source Generator, Polly v8, OpenTelemetry, xUnit v3

**Spec:** `Lib.Db/docs/superpowers/specs/2026-04-16-lib-db-v2.2-optimization-design.md`

**Base paths:**
- Runtime: `Lib.Db/Lib.Db/`
- Generator: `Lib.Db/Lib.Db.TvpGen/`
- Tests: `Lib.Db/Tests/Lib.Db.IntegrationTests/`
- Solution: `Lib.Db/Lib.Db.slnx`

---

## Track 1: TvpGen 버그 수정 (Critical 6 + Major 6)

### Task 1.1: Nullable<T> null 설정 코드 생성 수정 [T1-1, Critical]

**Files:**
- Modify: `Lib.Db/Lib.Db.TvpGen/ResultAccessorGenerator.cs:633-660`
- Test: `Lib.Db/Tests/Lib.Db.IntegrationTests/Binding/DataBindingTests.cs` (기존 테스트 확인)

- [ ] **Step 1: 현재 생성 코드의 Nullable 처리 문제 파악**

현재 `ResultAccessorGenerator.cs` 줄 637에서 생성하는 코드:
```csharp
// 현재 (버그): DB NULL이면 if 블록을 타지 않아 int?가 0으로 남음
sb.Append(indent).AppendLine($"            if (ord_{m.SafeName} != -1 && !r.IsDBNull(ord_{m.SafeName}))");
sb.Append(indent).AppendLine("            {");
// ... 값 할당 ...
sb.Append(indent).AppendLine("            }");
// else 없음 — Nullable<T> 프로퍼티는 new T()의 기본값(0)이 유지됨
```

- [ ] **Step 2: Nullable 여부 판별 로직 추가**

`ResultAccessorGenerator.cs`의 `Execute` 메서드 내 매핑 람다 생성 부분(줄 633~660)을 수정. `MappableMember`에 `IsNullableValueType` 정보를 추가:

```csharp
// CollectMappableMembers에서 MappableMember 생성 시 Nullable 여부 계산
bool isNullableValueType = typeSymbol.OriginalDefinition.SpecialType == SpecialType.System_Nullable_T;
```

- [ ] **Step 3: null 설정 else 블록 생성 코드 추가**

`ResultAccessorGenerator.cs` 줄 637~654 부분을 수정하여, Nullable value type일 때 else 블록에서 null을 설정:

```csharp
sb.Append(indent).AppendLine($"            if (ord_{m.SafeName} != -1 && !r.IsDBNull(ord_{m.SafeName}))");
sb.Append(indent).AppendLine("            {");

if (m.Kind == MemberKind.Property)
{
    if (m.RequiresUnsafeSetter)
        sb.Append(indent).AppendLine($"                __Set_{m.SafeName}(result, {expr});");
    else
        sb.Append(indent).AppendLine($"                result.{m.Name} = {expr};");
}
else
{
    if (m.RequiresUnsafeField)
        sb.Append(indent).AppendLine($"                __Field_{m.SafeName}(result) = {expr};");
    else
        sb.Append(indent).AppendLine($"                result.{m.Name} = {expr};");
}

sb.Append(indent).AppendLine("            }");

// v2.2 추가: Nullable value type일 때 DB NULL이면 명시적 null 설정
if (m.IsNullableValueType)
{
    sb.Append(indent).AppendLine("            else if (ord_{m.SafeName} != -1)");
    sb.Append(indent).AppendLine("            {");
    if (m.Kind == MemberKind.Property && m.RequiresUnsafeSetter)
        sb.Append(indent).AppendLine($"                __Set_{m.SafeName}(result, null);");
    else if (m.Kind == MemberKind.Field && m.RequiresUnsafeField)
        sb.Append(indent).AppendLine($"                __Field_{m.SafeName}(result) = null;");
    else
        sb.Append(indent).AppendLine($"                result.{m.Name} = null;");
    sb.Append(indent).AppendLine("            }");
}
```

- [ ] **Step 4: MappableMember에 IsNullableValueType 필드 추가**

`ResultAccessorGenerator.cs` 줄 856~863의 `MappableMember` record struct 수정:

```csharp
private readonly record struct MappableMember(
    string Name,
    string SafeName,
    ITypeSymbol Type,
    MemberKind Kind,
    bool RequiresUnsafeSetter,
    bool RequiresUnsafeField,
    bool IsNullableValueType,   // v2.2 추가
    Location Location);
```

- [ ] **Step 5: CollectMappableMembers에서 IsNullableValueType 전달**

`MappableMember` 생성 시 Nullable 판별:

```csharp
bool isNullableVT = propType.OriginalDefinition.SpecialType == SpecialType.System_Nullable_T;
// MappableMember 생성자에 isNullableVT 전달
```

- [ ] **Step 6: 빌드 검증**

Run: `dotnet build Lib.Db/Lib.Db.slnx`
Expected: 빌드 성공, 경고 0

- [ ] **Step 7: 기존 테스트 통과 확인**

Run: `dotnet test Lib.Db/Lib.Db.slnx --filter "FullyQualifiedName~Binding"`
Expected: 모든 테스트 PASS

- [ ] **Step 8: 커밋**

```bash
git add Lib.Db/Lib.Db.TvpGen/ResultAccessorGenerator.cs
git commit -m "fix(TvpGen): Nullable<T> 프로퍼티에 DB NULL 시 null 설정 코드 생성 (T1-1)"
```

---

### Task 1.2: date/time 역방향 매핑 수정 [T1-2, Critical]

**Files:**
- Modify: `Lib.Db/Lib.Db.TvpGen/TypeMappingRegistry.cs:271,276`

- [ ] **Step 1: MapSqlTypeToCSharp 매핑 수정**

`TypeMappingRegistry.cs` 줄 261~278의 switch expression 수정:

```csharp
// 변경 전:
"datetime" or "datetime2" or "date" => "DateTime",
"time" => "TimeSpan",

// 변경 후:
"datetime" or "datetime2" => "DateTime",
"date" => "DateOnly",
"time" => "TimeOnly",
```

- [ ] **Step 2: FullyQualifiedFormat 대응 확인**

`DbFirstTvpGenerator.GenerateClass`(줄 121)에서 `MapSqlTypeToCSharp` 반환값을 그대로 사용. `DateOnly`와 `TimeOnly`는 C# 키워드가 아니므로 `using System;`이 필요 — 이미 줄 104에서 생성하고 있으므로 OK.

- [ ] **Step 3: 빌드 검증**

Run: `dotnet build Lib.Db/Lib.Db.slnx`
Expected: 빌드 성공

- [ ] **Step 4: 커밋**

```bash
git add Lib.Db/Lib.Db.TvpGen/TypeMappingRegistry.cs
git commit -m "fix(TvpGen): date→DateOnly, time→TimeOnly 역방향 매핑 수정 (T1-2)"
```

---

### Task 1.3: ResultAccessorGenerator CompilationProvider.Combine 최적화 [T1-3, Critical]

**Files:**
- Modify: `Lib.Db/Lib.Db.TvpGen/ResultAccessorGenerator.cs:193-208`

- [ ] **Step 1: CompilationProvider에서 bool만 추출하여 Combine**

줄 193~208을 수정:

```csharp
public void Initialize(IncrementalGeneratorInitializationContext context)
{
    var candidates = context.SyntaxProvider
        .ForAttributeWithMetadataName(
            AttributeMetadataName,
            predicate: static (node, _) => node is TypeDeclarationSyntax,
            transform: static (ctx, _) => (INamedTypeSymbol)ctx.TargetSymbol)
        .WithComparer(SymbolEqualityComparer.Default);

    // v2.2: CompilationProvider 전체 대신 IMapableResult<T> 존재 여부 bool만 추출
    var hasIMapable = context.CompilationProvider
        .Select(static (comp, _) =>
            comp.GetTypeByMetadataName("Lib.Db.Contracts.Mapping.IMapableResult`1") is not null);

    var combined = candidates.Collect().Combine(hasIMapable);

    context.RegisterSourceOutput(
        combined,
        static (spc, pair) => Execute(spc, pair.Left, pair.Right));
}
```

- [ ] **Step 2: Execute 시그니처 수정**

`Execute` 메서드의 `Compilation` 파라미터를 `bool hasIMapable`로 변경:

```csharp
// 변경 전:
private static void Execute(SourceProductionContext spc, Compilation compilation, ImmutableArray<INamedTypeSymbol> types)

// 변경 후:
private static void Execute(SourceProductionContext spc, ImmutableArray<INamedTypeSymbol> types, bool hasIMapable)
```

Execute 본문에서 `compilation.GetTypeByMetadataName(...)` 호출을 `hasIMapable` 변수로 대체.

- [ ] **Step 3: 빌드 검증**

Run: `dotnet build Lib.Db/Lib.Db.slnx`
Expected: 빌드 성공

- [ ] **Step 4: 커밋**

```bash
git add Lib.Db/Lib.Db.TvpGen/ResultAccessorGenerator.cs
git commit -m "perf(TvpGen): CompilationProvider→bool 추출로 Generator 재실행 최소화 (T1-3)"
```

---

### Task 1.4: DbFirstTvpGenerator 빈 catch 블록 + Diagnostic 구현 [T1-4, Critical]

**Files:**
- Modify: `Lib.Db/Lib.Db.TvpGen/DbFirstTvpGenerator.cs:1-10,66-71,186-189`

- [ ] **Step 1: DiagnosticDescriptor 정의 추가**

파일 상단(줄 18 이후)에 진단 기술자 추가:

```csharp
private static readonly DiagnosticDescriptor SchemaNotFound = new(
    "LIBDB001",
    "스키마 파일 미발견",
    "libdb.schema.json 파일을 찾을 수 없습니다. AdditionalFiles에 추가하세요.",
    "Lib.Db.TvpGen",
    DiagnosticSeverity.Warning,
    isEnabledByDefault: true);

private static readonly DiagnosticDescriptor SchemaParseError = new(
    "LIBDB002",
    "스키마 파싱 실패",
    "libdb.schema.json 파싱 중 오류 발생: {0}",
    "Lib.Db.TvpGen",
    DiagnosticSeverity.Error,
    isEnabledByDefault: true);
```

- [ ] **Step 2: Execute 메서드에서 주석 처리된 진단 활성화 (줄 66~71)**

```csharp
// 변경 전 (줄 69-71):
else
{
    // 경고: 스키마를 찾을 수 없음
    // spc.ReportDiagnostic(...);
}

// 변경 후:
else
{
    spc.ReportDiagnostic(Diagnostic.Create(SchemaNotFound, Location.None));
}
```

- [ ] **Step 3: MiniJsonParser.Parse의 빈 catch 블록에 예외 정보 전달 (줄 186-189)**

```csharp
// 변경 전:
catch
{
    return null;
}

// 변경 후:
catch (Exception ex)
{
    return (null, ex.Message);  // 튜플 반환으로 변경하거나, 호출부에서 처리
}
```

또는 Parse 메서드를 `(SchemaRoot? root, string? error)` 튜플 반환으로 변경하고, Execute에서 error가 있으면 `spc.ReportDiagnostic(Diagnostic.Create(SchemaParseError, Location.None, error))` 호출.

- [ ] **Step 4: 빌드 검증**

Run: `dotnet build Lib.Db/Lib.Db.slnx`
Expected: 빌드 성공

- [ ] **Step 5: 커밋**

```bash
git add Lib.Db/Lib.Db.TvpGen/DbFirstTvpGenerator.cs
git commit -m "fix(TvpGen): DbFirstTvpGenerator 빈 catch 제거, Diagnostic 리포팅 구현 (T1-4)"
```

---

### Task 1.5: DbFirstTvpGenerator accessibility 데드코드 제거 + 파일 헤더 [T1-5, T1-6, Critical]

**Files:**
- Modify: `Lib.Db/Lib.Db.TvpGen/DbFirstTvpGenerator.cs:1-10,94-116`

- [ ] **Step 1: 표준 파일 헤더 + #nullable enable 추가 (줄 1~)**

```csharp
// ============================================================================
// 파일: Lib.Db.TvpGen/DbFirstTvpGenerator.cs
// 설명: 디자인 타임 스키마(libdb.schema.json) 기반 TVP DTO 자동 생성 제너레이터
// 대상: .NET 10 / C# 14
// ============================================================================

#nullable enable

using System.Collections.Generic;
// ... (기존 using)
```

- [ ] **Step 2: accessibility 데드코드 제거 (줄 98~100)**

```csharp
// 삭제할 코드:
var modifiers = symbol.DeclaredAccessibility.ToString().ToLowerInvariant();
if (modifiers == "internal") modifiers = "internal";
else modifiers = "public";
```

- [ ] **Step 3: GenerateClass에서 symbol.DeclaredAccessibility 반영 (줄 115)**

```csharp
// 변경 전:
sb.AppendLine($"    public partial class {className}");

// 변경 후:
string accessModifier = symbol.DeclaredAccessibility switch
{
    Accessibility.Internal => "internal",
    Accessibility.Public => "public",
    _ => "public"
};
sb.AppendLine($"    {accessModifier} partial class {className}");
```

- [ ] **Step 4: 빌드 검증 + 커밋**

Run: `dotnet build Lib.Db/Lib.Db.slnx`

```bash
git add Lib.Db/Lib.Db.TvpGen/DbFirstTvpGenerator.cs
git commit -m "fix(TvpGen): DbFirstTvpGenerator 파일 헤더, nullable, accessibility 수정 (T1-5,T1-6)"
```

---

### Task 1.6: Generator 공통 헬퍼 추출 [T1-7, T1-8, T1-9, Major]

**Files:**
- Create: `Lib.Db/Lib.Db.TvpGen/GeneratorSharedHelpers.cs`
- Modify: `Lib.Db/Lib.Db.TvpGen/TvpAccessorGenerator.cs:286-299,848-857,94`
- Modify: `Lib.Db/Lib.Db.TvpGen/ResultAccessorGenerator.cs:758-769,823-831,101`

- [ ] **Step 1: GeneratorSharedHelpers.cs 생성**

```csharp
// ============================================================================
// 파일: Lib.Db.TvpGen/GeneratorSharedHelpers.cs
// 설명: TvpAccessorGenerator와 ResultAccessorGenerator 공유 헬퍼
// 대상: .NET 10 / C# 14
// ============================================================================

#nullable enable

using Microsoft.CodeAnalysis;

namespace Lib.Db.TvpGen;

/// <summary>
/// Source Generator 간 공유 유틸리티 메서드입니다.
/// </summary>
internal static class GeneratorSharedHelpers
{
    /// <summary>Track 4 하이브리드 라우팅 임계값 (이 값 이하: else-if / 초과: FNV-1a + switch)</summary>
    internal const int SmallMemberThreshold = 12;

    /// <summary>
    /// 타입이 생성 코드에서 접근 가능한지 확인합니다.
    /// </summary>
    internal static bool IsAccessibleFromGeneratedCode(INamedTypeSymbol type)
    {
        INamedTypeSymbol? cur = type;
        while (cur is not null)
        {
            if (cur.DeclaredAccessibility is Accessibility.Private or Accessibility.Protected)
                return false;
            cur = cur.ContainingType;
        }
        return true;
    }

    /// <summary>
    /// 소스 제너레이터 AddSource에 사용될 안전한 파일 힌트명을 생성합니다.
    /// </summary>
    internal static string BuildSafeHintName(INamedTypeSymbol type, string suffix)
    {
        string name = type.ToDisplayString(SymbolDisplayFormat.FullyQualifiedFormat)
            .Replace("global::", "")
            .Replace("<", "_").Replace(">", "_")
            .Replace(".", "_")
            .Replace("+", "_");
        return name + suffix;
    }
}
```

- [ ] **Step 2: TvpAccessorGenerator에서 중복 코드를 헬퍼 호출로 교체**

- 줄 94: `private const int SmallMemberThreshold = 12;` 삭제 → `GeneratorSharedHelpers.SmallMemberThreshold` 사용
- 줄 286~299: `IsAccessibleFromGeneratedCode` 삭제 → `GeneratorSharedHelpers.IsAccessibleFromGeneratedCode` 호출
- 줄 848~857: `BuildSafeHintName` 삭제 → `GeneratorSharedHelpers.BuildSafeHintName` 호출
- 줄 864~865: `HashAsciiIgnoreCaseFnv1a` 래퍼 삭제 → 호출부에서 직접 `SharedHashUtils.HashAsciiIgnoreCaseFnv1a` 사용

- [ ] **Step 3: ResultAccessorGenerator에서 동일하게 교체**

- 줄 101: `SmallMemberThreshold` 삭제
- 줄 758~769: `IsAccessibleFromGeneratedCode` 삭제
- 줄 823~831: `BuildSafeHintName` 삭제
- 줄 844~845: `HashAsciiIgnoreCaseFnv1a` 래퍼 삭제

- [ ] **Step 4: 빌드 검증 + 커밋**

Run: `dotnet build Lib.Db/Lib.Db.slnx`

```bash
git add Lib.Db/Lib.Db.TvpGen/GeneratorSharedHelpers.cs Lib.Db/Lib.Db.TvpGen/TvpAccessorGenerator.cs Lib.Db/Lib.Db.TvpGen/ResultAccessorGenerator.cs
git commit -m "refactor(TvpGen): 공통 헬퍼 추출 — IsAccessibleFromGeneratedCode, BuildSafeHintName, SmallMemberThreshold (T1-7,T1-8,T1-9)"
```

---

### Task 1.7: MappableMember IEquatable 구현 + GeneratedRegex 적용 [T1-10, T1-12, Major]

**Files:**
- Modify: `Lib.Db/Lib.Db.TvpGen/ResultAccessorGenerator.cs:856-863`
- Modify: `Lib.Db/Lib.Db.TvpGen/DbFirstTvpGenerator.cs:203,211,233,244`

- [ ] **Step 1: MappableMember에 커스텀 Equals 구현**

```csharp
private readonly record struct MappableMember(
    string Name,
    string SafeName,
    string TypeFullName,        // ITypeSymbol 대신 string으로 변환하여 저장
    MemberKind Kind,
    bool RequiresUnsafeSetter,
    bool RequiresUnsafeField,
    bool IsNullableValueType,
    string LocationPath) : IEquatable<MappableMember>;
```

`ITypeSymbol`을 `string TypeFullName`으로 변환하여 저장하면 `record struct`의 기본 `Equals`가 `string.Equals`를 사용하므로 `SymbolEqualityComparer` 없이도 올바른 비교가 가능. `Location`도 `string LocationPath`로 변환.

- [ ] **Step 2: CollectMappableMembers에서 TypeFullName 전달**

MappableMember 생성 시:
```csharp
string typeFullName = typeSymbol.ToDisplayString(SymbolDisplayFormat.FullyQualifiedFormat);
string locationPath = location.GetLineSpan().Path;
```

- [ ] **Step 3: GetReaderValueExpression 호출부에서 TypeFullName 사용하도록 수정**

TypeMappingRegistry.GetReaderValueExpression이 ITypeSymbol을 받으므로, 별도 경로 필요. 생성 시 미리 expression을 계산하여 MappableMember에 `ReaderExpression` string으로 저장하는 방식 검토.

- [ ] **Step 4: DbFirstTvpGenerator에 [GeneratedRegex] 적용**

```csharp
// DbFirstTvpGenerator.cs 상단에 추가
[GeneratedRegex("\"Tvps\"\\s*:\\s*\\{")]
private static partial Regex TvpsSectionRegex();

[GeneratedRegex("\"([^\"]+)\"\\s*:\\s*\\[(.*?)\\]", RegexOptions.Singleline)]
private static partial Regex TvpKeyArrayRegex();

[GeneratedRegex("\\{(.*?)\\}", RegexOptions.Singleline)]
private static partial Regex ColumnObjectRegex();

[GeneratedRegex("\"([^\"]+)\"\\s*:\\s*\"([^\"]+)\"")]
private static partial Regex PropertyKeyValueRegex();
```

MiniJsonParser 내의 `System.Text.RegularExpressions.Regex.Match/Matches` 호출을 위 정적 메서드로 교체.

- [ ] **Step 5: 빌드 검증 + 커밋**

Run: `dotnet build Lib.Db/Lib.Db.slnx`

```bash
git add Lib.Db/Lib.Db.TvpGen/ResultAccessorGenerator.cs Lib.Db/Lib.Db.TvpGen/DbFirstTvpGenerator.cs
git commit -m "perf(TvpGen): MappableMember Equatable 최적화, GeneratedRegex 적용 (T1-10,T1-12)"
```

---

### Task 1.8: DbFirstTvpGenerator FullyQualifiedFormat 적용 [T1-11, Major]

**Files:**
- Modify: `Lib.Db/Lib.Db.TvpGen/TypeMappingRegistry.cs:261-278`

- [ ] **Step 1: MapSqlTypeToCSharp 반환값을 fully qualified 형식으로 변경**

```csharp
public static string MapSqlTypeToCSharp(string sqlType)
{
    return sqlType.ToLowerInvariant() switch
    {
        "bigint"         => "long",
        "int"            => "int",
        "smallint"       => "short",
        "tinyint"        => "byte",
        "bit"            => "bool",
        "decimal" or "numeric" or "money" => "decimal",
        "float"          => "double",
        "real"           => "float",
        "datetime" or "datetime2" => "global::System.DateTime",
        "date"           => "global::System.DateOnly",
        "datetimeoffset" => "global::System.DateTimeOffset",
        "varchar" or "nvarchar" or "char" or "nchar" or "text" or "ntext" => "string",
        "uniqueidentifier" => "global::System.Guid",
        "binary" or "varbinary" or "image" => "byte[]",
        "time"           => "global::System.TimeOnly",
        _                => "object"
    };
}
```

- [ ] **Step 2: 빌드 검증 + 커밋**

Run: `dotnet build Lib.Db/Lib.Db.slnx`

```bash
git add Lib.Db/Lib.Db.TvpGen/TypeMappingRegistry.cs
git commit -m "fix(TvpGen): MapSqlTypeToCSharp FullyQualifiedFormat 적용 (T1-11)"
```

---

### Task 1.9: Track 1 전체 검증

- [ ] **Step 1: 전체 빌드**

Run: `dotnet build Lib.Db/Lib.Db.slnx`
Expected: 빌드 성공, 경고 0

- [ ] **Step 2: 단위 테스트**

Run: `dotnet test Lib.Db/Lib.Db.slnx --filter "FullyQualifiedName~Binding or FullyQualifiedName~Aot or FullyQualifiedName~Unit"`
Expected: 모든 테스트 PASS

---

## Track 2: Lib.Db 메모리/안전성 (Critical 4 + Major 7)

### Task 2.1: Meter/ActivitySource 단일 중앙 집중화 [T2-1, Critical]

**Files:**
- Modify: `Lib.Db/Lib.Db/Diagnostics/LibDbTelemetry.cs:24,29`
- Modify: `Lib.Db/Lib.Db/Diagnostics/DbDiagnostics.cs:254-258`
- Modify: `Lib.Db/Lib.Db/Execution/Executors/SqlDbExecutor.cs:50-52`
- Modify: `Lib.Db/Lib.Db/Schema/SchemaFlushService.cs:30`
- Modify: `Lib.Db/Lib.Db/Schema/SchemaService.cs:109`

- [ ] **Step 1: LibDbTelemetry에 버전 추가, 단일 Meter로 통합**

```csharp
// LibDbTelemetry.cs
public static readonly ActivitySource ActivitySource = new(SourceName, "2.2.0");
public static readonly Meter Meter = new(SourceName, "2.2.0");
```

- [ ] **Step 2: DbDiagnostics.DbMetrics의 s_meter 제거**

`DbDiagnostics.cs` 줄 258의 `private static readonly Meter s_meter = new(MeterName, MeterVersion);`를 삭제하고, 모든 `s_meter` 사용을 `LibDbTelemetry.Meter`로 교체.

- [ ] **Step 3: SqlDbExecutor의 s_activitySource 제거**

`SqlDbExecutor.cs` 줄 52의 `private static readonly ActivitySource s_activitySource = new(ActivitySourceName);`를 삭제하고, `s_activitySource` 사용을 `LibDbTelemetry.ActivitySource`로 교체.

- [ ] **Step 4: SchemaService, SchemaFlushService의 개별 ActivitySource 제거**

각 파일의 `private static readonly ActivitySource s_activity = new(...)` 삭제 → `LibDbTelemetry.ActivitySource` 사용.

- [ ] **Step 5: 빌드 검증 + 커밋**

Run: `dotnet build Lib.Db/Lib.Db.slnx`

```bash
git add Lib.Db/Lib.Db/Diagnostics/ Lib.Db/Lib.Db/Execution/Executors/SqlDbExecutor.cs Lib.Db/Lib.Db/Schema/
git commit -m "fix: Meter/ActivitySource 단일 중앙 집중화 — 이중 등록 제거 (T2-1)"
```

---

### Task 2.2: NegativeCache Count+Clear 비원자성 수정 [T2-2, Critical]

**Files:**
- Modify: `Lib.Db/Lib.Db/Schema/NegativeCache.cs:76-92`

- [ ] **Step 1: Interlocked 카운터 기반으로 변경**

```csharp
private static int s_count;  // Interlocked로 관리

[MethodImpl(MethodImplOptions.AggressiveInlining)]
public static void RecordMissing(string dbHash, string objectName, string objectType)
{
    // 카운터 기반 제한 (비원자 Clear 대신 개별 항목 관리)
    if (Interlocked.Increment(ref s_count) > s_maxSize)
    {
        // 최대 크기 초과 시 오래된 항목 대신 전체 Clear
        // 다중 스레드가 동시에 진입해도 Clear가 여러 번 호출되는 것은 안전
        s_missingObjects.Clear();
        Interlocked.Exchange(ref s_count, 0);
    }

    string key = BuildKey(dbHash, objectName, objectType);
    InvalidOperationException ex = new InvalidOperationException(
        $"[Negative Cache] {objectType} '{objectName}'이(가) DB '{dbHash}'에 존재하지 않습니다.");
    s_missingObjects[key] = ex;
}
```

- [ ] **Step 2: 빌드 검증 + 커밋**

```bash
git add Lib.Db/Lib.Db/Schema/NegativeCache.cs
git commit -m "fix: NegativeCache Count+Clear 비원자성 수정 — Interlocked 카운터 도입 (T2-2)"
```

---

### Task 2.3: CacheBytesFreed 실제 값 연결 + BuildServiceProvider 제거 [T2-3, T2-4, Critical]

**Files:**
- Modify: `Lib.Db/Lib.Db/Diagnostics/LibDbTelemetry.cs:76-81`
- Modify: `Lib.Db/Lib.Db/Extensions/LibDbServiceCollectionExtensions.cs:174-220`

- [ ] **Step 1: CacheBytesFreed에 실제 콜백 연결**

```csharp
// LibDbTelemetry.cs
private static long s_totalBytesFreed;

public static void RecordBytesFreed(long bytes) =>
    Interlocked.Add(ref s_totalBytesFreed, bytes);

public static readonly ObservableGauge<long> CacheBytesFreed = Meter.CreateObservableGauge<long>(
    "libdb.cache_bytes_freed",
    () => Interlocked.Read(ref s_totalBytesFreed),
    unit: "bytes",
    description: "캐시 정리 사이클에서 해제된 총 바이트");
```

- [ ] **Step 2: BuildServiceProvider 이중 호출 제거**

`LibDbServiceCollectionExtensions.cs`의 `AddLibDbHostedServices`(줄 178)와 `AddSchemaFlushCoordination`(줄 219)에서 `BuildServiceProvider()` 제거. `IOptions<LibDbOptions>` 지연 평가로 교체:

```csharp
// 변경 전:
using ServiceProvider sp = services.BuildServiceProvider();
LibDbOptions? options = sp.GetService<LibDbOptions>();

// 변경 후: Post-configure 패턴 사용
// 옵션을 직접 확인하는 대신, 조건부 등록을 HostedService 내부로 이동
// 또는 IConfigureOptions<T> 패턴 사용
services.TryAddEnumerable(
    ServiceDescriptor.Singleton<IHostedService, SchemaWarmupService>());
// SchemaWarmupService 내부에서 IOptions<LibDbOptions>를 주입받아 조건 확인
```

- [ ] **Step 3: 빌드 검증 + 커밋**

```bash
git add Lib.Db/Lib.Db/Diagnostics/LibDbTelemetry.cs Lib.Db/Lib.Db/Extensions/LibDbServiceCollectionExtensions.cs
git commit -m "fix: CacheBytesFreed 실제 콜백 연결, BuildServiceProvider 이중 호출 제거 (T2-3,T2-4)"
```

---

### Task 2.4: MemoryMonitoring + SharedMemoryCache + s_marsEnabledCache 수정 [T2-5,T2-6,T2-7, Major]

**Files:**
- Modify: `Lib.Db/Lib.Db/Infrastructure/MemoryMonitoring.cs:28-29,51`
- Modify: `Lib.Db/Lib.Db/Caching/SharedMemoryCache.cs:114`
- Modify: `Lib.Db/Lib.Db/Execution/Executors/SqlDbExecutor.cs:59`

- [ ] **Step 1: MemoryMonitoring — Volatile + Stopwatch**

```csharp
// 변경 전:
private double _cachedLoadFactor;
private long _lastCheckTick;
long now = DateTime.UtcNow.Ticks;

// 변경 후:
private double _cachedLoadFactor;
private long _lastCheckTimestamp;  // Stopwatch 기반
private static readonly long s_cacheDurationTimestamp =
    (long)(500.0 / 1000.0 * Stopwatch.Frequency); // 500ms

// LoadFactor getter:
long now = Stopwatch.GetTimestamp();
if (now - Volatile.Read(ref _lastCheckTimestamp) > s_cacheDurationTimestamp)
{
    GCMemoryInfo info = GC.GetGCMemoryInfo();
    long total = info.TotalAvailableMemoryBytes;
    long used = info.MemoryLoadBytes;
    Volatile.Write(ref _cachedLoadFactor, total > 0 ? (double)used / total : 0.0);
    Volatile.Write(ref _lastCheckTimestamp, now);
}
return Volatile.Read(ref _cachedLoadFactor);
```

- [ ] **Step 2: SharedMemoryCache Fallback guard**

```csharp
// SharedMemoryCache.cs 내 Mutex 접근 코드에 guard 추가
if (_isFallbackMode)
{
    // Fallback 모드에서는 Mutex 없이 직접 처리하거나 no-op
    return;
}
// 기존 Mutex 접근 코드
```

- [ ] **Step 3: s_marsEnabledCache MaxSize 제한**

```csharp
// SqlDbExecutor.cs
private static readonly ConcurrentDictionary<string, bool> s_marsEnabledCache = new();
private const int MarsMaxCacheSize = 64;

// ValidateMarsEnabled 내:
if (s_marsEnabledCache.Count >= MarsMaxCacheSize)
{
    // 가장 오래된 항목을 제거할 수 없으므로, 전체 Clear 후 현재 항목만 추가
    s_marsEnabledCache.Clear();
}
s_marsEnabledCache[connStr] = isEnabled;
```

- [ ] **Step 4: 빌드 검증 + 커밋**

```bash
git add Lib.Db/Lib.Db/Infrastructure/MemoryMonitoring.cs Lib.Db/Lib.Db/Caching/SharedMemoryCache.cs Lib.Db/Lib.Db/Execution/Executors/SqlDbExecutor.cs
git commit -m "fix: MemoryMonitoring volatile/Stopwatch, SharedMemoryCache fallback guard, MARS cache MaxSize (T2-5,T2-6,T2-7)"
```

---

### Task 2.5: AdaptivePolicyFactory 제거 + Strategies race condition + HealthCheck + OTel 플래그 [T2-8~T2-11, Major]

**Files:**
- Delete: `Lib.Db/Lib.Db/Infrastructure/Resilience/AdaptivePolicyFactory.cs`
- Modify: `Lib.Db/Lib.Db/Execution/Executors/Strategies.cs:446`
- Modify: `Lib.Db/Lib.Db/Extensions/LibDbHealthCheckExtensions.cs:45-99`
- Modify: `Lib.Db/Lib.Db/Configuration/LibDbOptions.cs:638-644`

- [ ] **Step 1: AdaptivePolicyFactory.cs 삭제**

DI에 등록되지 않은 dead code. 파일 전체 삭제.

- [ ] **Step 2: _elevatePriorityOnNextRetry → AsyncLocal**

```csharp
// Strategies.cs
// 변경 전:
private bool _elevatePriorityOnNextRetry;

// 변경 후:
private static readonly AsyncLocal<bool> s_elevatePriorityOnNextRetry = new();
```

사용부에서 `_elevatePriorityOnNextRetry` → `s_elevatePriorityOnNextRetry.Value`로 변경.

- [ ] **Step 3: HealthCheck static → instance**

```csharp
// LibDbHealthCheckExtensions.cs
// 변경 전:
private static HealthCheckResult s_lastResult = ...;
private static long s_lastCheckTick;
private static readonly long s_throttleTicks = TimeSpan.FromSeconds(1).Ticks;

// 변경 후:
private HealthCheckResult _lastResult = HealthCheckResult.Healthy("Initial State");
private long _lastCheckTick;
private readonly long _throttleTicks;

// 생성자에서:
_throttleTicks = TimeSpan.FromSeconds(options.HealthCheckThrottleSeconds).Ticks;
```

- [ ] **Step 4: EnableOpenTelemetry → [Obsolete]**

```csharp
// LibDbOptions.cs
[Obsolete("EnableObservability를 사용하세요. 이 속성은 v3.0에서 제거됩니다.")]
public bool EnableOpenTelemetry
{
    get => EnableObservability;
    set => EnableObservability = value;
}

public bool EnableObservability { get; set; } = false;
```

- [ ] **Step 5: 빌드 검증 + 전체 테스트**

Run: `dotnet build Lib.Db/Lib.Db.slnx && dotnet test Lib.Db/Lib.Db.slnx --filter "FullyQualifiedName~Unit or FullyQualifiedName~Diagnostics or FullyQualifiedName~Caching"`

- [ ] **Step 6: 커밋**

```bash
git add -A Lib.Db/Lib.Db/
git commit -m "fix: AdaptivePolicyFactory 제거, Strategies AsyncLocal, HealthCheck instance화, OTel 플래그 통합 (T2-8~T2-11)"
```

---

### Task 2.6: Track 2 전체 검증

- [ ] **Step 1: 전체 빌드 + 테스트**

Run: `dotnet build Lib.Db/Lib.Db.slnx && dotnet test Lib.Db/Lib.Db.slnx --filter "FullyQualifiedName~Unit or FullyQualifiedName~Caching or FullyQualifiedName~Diagnostics or FullyQualifiedName~Executors"`
Expected: 모든 테스트 PASS

---

## Track 3: 중복 제거 + 아키텍처 (Major 10건)

### Task 3.1: ExecutionStage 실행 메서드 중복 제거 [T3-1]

**Files:**
- Create: `Lib.Db/Lib.Db/Fluent/ExecutionHelper.cs`
- Modify: `Lib.Db/Lib.Db/Fluent/DbRequestBuilder.cs`

- [ ] **Step 1: ExecutionHelper static 클래스 생성**

`FormattableStringParameterStage`와 `ExecutionStage<TParams>` 간 공유하는 예외→DbResult 변환 로직을 `ExecutionHelper`로 추출:

```csharp
internal static class ExecutionHelper
{
    internal static async Task<DbResult<T>> ExecuteWithErrorHandling<T>(
        Func<Task<T>> operation,
        DbRequestInfo requestInfo,
        ILogger logger)
    {
        try
        {
            T result = await operation().ConfigureAwait(false);
            return DbResult<T>.Ok(result);
        }
        catch (SqlException ex)
        {
            return DbResult<T>.Fail(DbErrorMapper.Map(ex));
        }
        catch (Exception ex)
        {
            return DbResult<T>.Fail(DbError.FromException(ex));
        }
    }
}
```

- [ ] **Step 2: FormattableStringParameterStage와 ExecutionStage에서 ExecutionHelper 호출로 교체**

5개 실행 메서드(QueryAsync, QuerySingleAsync, ExecuteScalarAsync, QueryMultipleAsync, ExecuteAsync) 모두 `ExecutionHelper.ExecuteWithErrorHandling` 위임.

- [ ] **Step 3: 빌드 검증 + 커밋**

```bash
git add Lib.Db/Lib.Db/Fluent/
git commit -m "refactor: ExecutionStage 실행 메서드 중복 제거 — ExecutionHelper 추출 (T3-1)"
```

---

### Task 3.2: SchemaService 제네릭 통합 + 오류 코드 일관성 [T3-2, T3-7]

**Files:**
- Modify: `Lib.Db/Lib.Db/Schema/SchemaService.cs:150-286`
- Modify: `Lib.Db/Lib.Db/Diagnostics/DbErrorMapper.cs`

- [ ] **Step 1: GetSchemaAsync<TSchema> 제네릭 메서드로 통합**

`GetSpSchemaAsync`/`GetTvpSchemaAsync`의 공통 3단계 캐시 패턴을 제네릭 메서드 하나로:

```csharp
private async Task<TSchema?> GetSchemaAsync<TSchema>(
    string key,
    ConcurrentDictionary<string, TSchema> l2Cache,
    Func<string, string, CancellationToken, Task<TSchema?>> loadFromDb,
    CancellationToken ct) where TSchema : class
{
    // L1 FrozenDictionary 조회 → L2 ConcurrentDictionary → HybridCache → DB
    // ... 공통 로직 ...
}
```

- [ ] **Step 2: DbErrorMapper에 누락 오류 코드 추가**

```csharp
// 오류 코드 53 (서버 미발견) → ConnectionLost
// 오류 코드 49918 (Azure transient) → CloudTransient
```

- [ ] **Step 3: 빌드 검증 + 커밋**

```bash
git add Lib.Db/Lib.Db/Schema/SchemaService.cs Lib.Db/Lib.Db/Diagnostics/DbErrorMapper.cs
git commit -m "refactor: SchemaService 제네릭 통합, DbErrorMapper 누락 오류코드 추가 (T3-2,T3-7)"
```

---

### Task 3.3: 나머지 중복 제거 [T3-3~T3-6, T3-8~T3-10]

**Files:**
- Modify: `Lib.Db/Lib.Db/Execution/Executors/Strategies.cs`
- Modify: `Lib.Db/Lib.Db/Extensions/QueryCacheExtensions.cs`
- Modify: `Lib.Db/Lib.Db/Diagnostics/DbDiagnostics.cs`
- Modify: `Lib.Db/Lib.Db.TvpGen/TvpGenFeatureTokens.cs`
- Modify: `Lib.Db/Tests/Lib.Db.IntegrationTests/Lib.Db.IntegrationTests.csproj`

- [ ] **Step 1: Strategies — PrepareConnectionAsync 공통 메서드 추출 (T3-3)**
- [ ] **Step 2: QueryCacheExtensions — SerializeAndCacheAsync 추출 (T3-4)**
- [ ] **Step 3: DbDiagnostics — WithScopeInfo 고차 함수 (T3-6)**
- [ ] **Step 4: TvpGenFeatureTokens public → internal (T3-9)**
- [ ] **Step 5: IntegrationTests.csproj TvpGen 중복 참조 제거 (T3-10)**

L27의 일반 ProjectReference 삭제, L26의 `OutputItemType="Analyzer"` 참조만 유지.

- [ ] **Step 6: 빌드 검증 + 전체 테스트 + 커밋**

Run: `dotnet build Lib.Db/Lib.Db.slnx && dotnet test Lib.Db/Lib.Db.slnx`

```bash
git add -A
git commit -m "refactor: Strategies/QueryCache/DbDiagnostics 중복 제거, TvpGenFeatureTokens internal 전환, 중복 참조 제거 (T3-3~T3-10)"
```

---

## Track 4: 성능 최적화 (Major 6 + Minor 4)

### Task 4.1: ObjectDataReader Expression Tree 컴파일 [T4-1, Major]

**Files:**
- Modify: `Lib.Db/Lib.Db/Execution/Bulk/ObjectDataReader.cs:29-55`

- [ ] **Step 1: PropertyInfo.GetValue → 컴파일된 Func<T,object> 배열로 교체**

```csharp
[RequiresUnreferencedCode("ObjectDataReader는 Reflection을 사용하여 T의 속성을 열거합니다.")]
internal sealed class ObjectDataReader<T>(IEnumerator<T> enumerator, PropertyInfo[] properties)
    : IDataReader
{
    private static readonly ConcurrentDictionary<Type, Func<object, object?>[]> s_getterCache = new();

    private readonly Func<object, object?>[] _getters = s_getterCache.GetOrAdd(
        typeof(T),
        static type => BuildGetters(type));

    private static Func<object, object?>[] BuildGetters(Type type)
    {
        PropertyInfo[] props = type.GetProperties(BindingFlags.Instance | BindingFlags.Public);
        Func<object, object?>[] getters = new Func<object, object?>[props.Length];

        for (int i = 0; i < props.Length; i++)
        {
            ParameterExpression param = Expression.Parameter(typeof(object), "obj");
            UnaryExpression cast = Expression.Convert(param, type);
            MemberExpression access = Expression.Property(cast, props[i]);
            UnaryExpression box = Expression.Convert(access, typeof(object));
            getters[i] = Expression.Lambda<Func<object, object?>>(box, param).Compile();
        }
        return getters;
    }

    public object GetValue(int i) => _getters[i](enumerator.Current!) ?? DBNull.Value;
}
```

- [ ] **Step 2: 빌드 검증 + 커밋**

```bash
git add Lib.Db/Lib.Db/Execution/Bulk/ObjectDataReader.cs
git commit -m "perf: ObjectDataReader Expression Tree getter 캐싱 — Reflection 제거 (T4-1)"
```

---

### Task 4.2: SqlInterpolatedStringHandler @p 캐싱 + 기타 성능 [T4-3~T4-9]

**Files:**
- Modify: `Lib.Db/Lib.Db/Fluent/SqlInterpolatedStringHandler.cs:108`
- Modify: `Lib.Db/Lib.Db/Schema/SchemaService.cs:843`
- Modify: `Lib.Db/Lib.Db/Execution/Executors/SqlGridReader.cs:121`
- Modify: `Lib.Db/Lib.Db/Execution/Binding/Mappers.cs:175`
- Modify: `Lib.Db/Lib.Db/Core/DbSession.cs:274`
- Modify: `Lib.Db/Lib.Db/Core/Runtime.cs:167`

- [ ] **Step 1: @p0~@p9 사전 캐싱**

```csharp
// SqlInterpolatedStringHandler.cs
private static readonly string[] s_paramNames =
    Enumerable.Range(0, 10).Select(i => $"@p{i}").ToArray();

public void AppendFormatted<T>(T value)
{
    if (_buffer == null || !_isValid) return;

    int idx = _parameterIndex++;
    string paramName = idx < s_paramNames.Length
        ? s_paramNames[idx]
        : $"@p{idx}";
    // ... 나머지 동일
}
```

- [ ] **Step 2: SchemaService.Clear() — .Keys.ToArray() 제거**

```csharp
// 변경 전: foreach (var key in _l2Sp.Keys.ToArray()) _l2Sp.TryRemove(key, out _);
// 변경 후:
_l2Sp.Clear();
_l2Tvp.Clear();
```

- [ ] **Step 3: SqlGridReader.ReadEmptyAsync — static 빈 리스트**

```csharp
// 변경 전: return Task.FromResult(new List<T>());
// 변경 후:
return Task.FromResult<IReadOnlyList<T>>([]);
```

- [ ] **Step 4: Mappers.CleanupGen0Cache — 단일 순회로 통합**

- [ ] **Step 5: DbSession.SHA256 — stackalloc**

```csharp
// 변경 전: SHA256.HashData(System.Text.Encoding.UTF8.GetBytes(connectionString), hashBytes);
// 변경 후:
Span<byte> utf8Bytes = stackalloc byte[Encoding.UTF8.GetMaxByteCount(connectionString.Length)];
int written = Encoding.UTF8.GetBytes(connectionString, utf8Bytes);
SHA256.HashData(utf8Bytes[..written], hashBytes);
```

- [ ] **Step 6: Runtime.cs — System.Threading.Lock**

```csharp
// 변경 전: private static readonly object s_sync = new();
// 변경 후:
private static readonly Lock s_sync = new();
```

- [ ] **Step 7: 빌드 검증 + 전체 테스트 + 커밋**

Run: `dotnet build Lib.Db/Lib.Db.slnx && dotnet test Lib.Db/Lib.Db.slnx --filter "FullyQualifiedName~Unit or FullyQualifiedName~Stress"`

```bash
git add -A Lib.Db/Lib.Db/
git commit -m "perf: @p 캐싱, SchemaService.Clear, ReadEmpty, CleanupGen0, SHA256 stackalloc, Lock 타입 (T4-3~T4-9)"
```

---

### Task 4.3: SharedMemoryCache.Refresh 구현 [T4-10]

**Files:**
- Modify: `Lib.Db/Lib.Db/Caching/SharedMemoryCache.cs:384-394`

- [ ] **Step 1: Refresh 실제 구현 또는 NotSupportedException**

```csharp
public override void Refresh(string key)
{
    // SharedMemoryCache는 절대 만료(Absolute Expiration)만 지원
    // Sliding Expiration은 MMF 기반 구조에서 지원하지 않으므로 명시적 미지원
    // 호출되어도 무시 (IDistributedCache 계약상 no-op 허용)
}
```

만약 Sliding Expiration 지원이 필요하다면 MMF 헤더의 ExpiryTicks를 현재 시간 + SlidingWindow로 갱신하는 로직 구현 필요. 현재는 no-op 유지하되 주석으로 의도 명시.

- [ ] **Step 2: 커밋**

```bash
git add Lib.Db/Lib.Db/Caching/SharedMemoryCache.cs
git commit -m "docs: SharedMemoryCache.Refresh no-op 의도 문서화 (T4-10)"
```

---

## Track 5: 한글 주석 + 코드 표준화 (Minor 10건)

### Task 5.1: LibDbTelemetry + DbDiagnostics + CacheMaintenanceService XML doc [T5-1~T5-3]

**Files:**
- Modify: `Lib.Db/Lib.Db/Diagnostics/LibDbTelemetry.cs`
- Modify: `Lib.Db/Lib.Db/Diagnostics/DbDiagnostics.cs`
- Modify: `Lib.Db/Lib.Db/Caching/CacheMaintenanceService.cs`

- [ ] **Step 1: LibDbTelemetry 9개 메트릭 속성에 Korean <summary> 추가**

각 `Counter`/`Histogram`/`ObservableGauge` 속성에:
```csharp
/// <summary>
/// SqlDbExecutor에서 실행된 총 DB 요청 수를 추적하는 카운터입니다.
/// </summary>
public static readonly Counter<long> DbRequestsTotal = ...;
```

- [ ] **Step 2: DbDiagnostics 내부 로거 클래스에 Korean <summary>**
- [ ] **Step 3: CacheMaintenanceService.PerformMaintenanceAsync에 Korean <summary>**
- [ ] **Step 4: 커밋**

```bash
git add Lib.Db/Lib.Db/Diagnostics/ Lib.Db/Lib.Db/Caching/CacheMaintenanceService.cs
git commit -m "docs: LibDbTelemetry, DbDiagnostics, CacheMaintenanceService 한글 XML doc 추가 (T5-1~T5-3)"
```

---

### Task 5.2: TvpGen 한글 주석 + sealed + 파일 헤더 [T5-4~T5-7]

**Files:**
- Modify: `Lib.Db/Lib.Db.TvpGen/DbFirstTvpGenerator.cs`
- Modify: `Lib.Db/Lib.Db.TvpGen/StaticBakingGenerator.cs`
- Modify: `Lib.Db/Lib.Db.TvpGen/TvpGenFeatureTokens.cs`

- [ ] **Step 1: DbFirstTvpGenerator — Initialize/Execute/GenerateClass/ToPascalCase XML doc**
- [ ] **Step 2: SchemaRoot, ColumnDef에 sealed + <summary>**
- [ ] **Step 3: StaticBakingGenerator 4개 메서드 XML doc**
- [ ] **Step 4: TvpGenFeatureTokens 표준 파일 헤더**
- [ ] **Step 5: 커밋**

```bash
git add Lib.Db/Lib.Db.TvpGen/
git commit -m "docs: TvpGen 한글 XML doc 완비, sealed 적용, 파일 헤더 표준화 (T5-4~T5-7)"
```

---

### Task 5.3: sealed 미적용 + Substring→range + 파일 헤더 이중 주석 [T5-8~T5-10]

**Files:**
- Modify: `Lib.Db/Lib.Db/Schema/SchemaService.cs` (HybridSchemaSnapshot sealed)
- Modify: `Lib.Db/Lib.Db/Caching/CachingInfrastructure.cs:73,179`
- Modify: `Lib.Db/Lib.Db/Infrastructure/DbExecutorFactory.cs:9-11`

- [ ] **Step 1: 상속 불필요 클래스에 sealed 추가**
- [ ] **Step 2: .Substring(0, N) → [..N] range 슬라이싱**
- [ ] **Step 3: DbExecutorFactory.cs 파일 헤더 중복 닫는 줄 제거**
- [ ] **Step 4: 빌드 검증 + 커밋**

```bash
git add -A Lib.Db/Lib.Db/
git commit -m "style: sealed 적용, Substring→range, 파일 헤더 중복 제거 (T5-8~T5-10)"
```

---

## Track 6: MARS 정책 통합 (신규 7건)

### Task 6.1: MarsPolicy 열거형 + LibDbOptions 속성 추가 [T6-1, T6-2]

**Files:**
- Create: `Lib.Db/Lib.Db/Configuration/MarsPolicy.cs`
- Modify: `Lib.Db/Lib.Db/Configuration/LibDbOptions.cs`

- [ ] **Step 1: MarsPolicy 열거형 생성**

```csharp
// ============================================================================
// 파일: Lib.Db/Configuration/MarsPolicy.cs
// 설명: MARS(다중 활성 결과 집합) 정책 열거형
// 대상: .NET 10 / C# 14
// ============================================================================

#nullable enable

namespace Lib.Db.Configuration;

/// <summary>
/// MARS(Multiple Active Result Sets) 정책을 지정합니다.
/// <para>
/// <b>[설계 의도]</b><br/>
/// ConnectionString에 <c>MultipleActiveResultSets=True</c> 설정을 라이브러리 수준에서 관리합니다.
/// </para>
/// </summary>
public enum MarsPolicy
{
    /// <summary>
    /// MARS를 사용하지 않습니다.
    /// <para>QueryMultipleAsync 호출 시 <see cref="InvalidOperationException"/> 발생.</para>
    /// </summary>
    Disabled,

    /// <summary>
    /// 자동 감지 모드 (기본값).
    /// <para>QueryMultipleAsync 사용 시 MARS 미설정이면 경고 로그 후 예외.</para>
    /// </summary>
    Auto,

    /// <summary>
    /// 강제 활성화.
    /// <para>AddLibDb() 등록 시점에서 ConnectionString에 MARS가 없으면 자동 주입합니다.</para>
    /// </summary>
    ForceEnable
}
```

- [ ] **Step 2: LibDbOptions에 Mars 속성 추가**

```csharp
// LibDbOptions.cs — [1] 연결 및 인프라 설정 region 내
/// <summary>
/// MARS(다중 활성 결과 집합) 정책입니다. (기본값: <see cref="MarsPolicy.Auto"/>)
/// <para>
/// <b>[설계 의도]</b><br/>
/// <c>ForceEnable</c> 설정 시 AddLibDb() 등록 시점에서 ConnectionString에
/// <c>MultipleActiveResultSets=True</c>가 자동 주입됩니다.
/// </para>
/// </summary>
public MarsPolicy Mars { get; set; } = MarsPolicy.Auto;
```

- [ ] **Step 3: 빌드 검증 + 커밋**

```bash
git add Lib.Db/Lib.Db/Configuration/MarsPolicy.cs Lib.Db/Lib.Db/Configuration/LibDbOptions.cs
git commit -m "feat: MarsPolicy 열거형 및 LibDbOptions.Mars 속성 추가 (T6-1,T6-2)"
```

---

### Task 6.2: ForceEnable 연결 문자열 자동 보정 [T6-3]

**Files:**
- Modify: `Lib.Db/Lib.Db/Extensions/LibDbServiceCollectionExtensions.cs`

- [ ] **Step 1: AddLibDbOptions 등록 시점에서 MARS 자동 주입**

```csharp
// ForceEnable 모드: ConnectionString에 MARS 자동 추가
if (options.Mars == MarsPolicy.ForceEnable)
{
    Dictionary<string, string> corrected = new(options.ConnectionStrings.Count);
    foreach (KeyValuePair<string, string> kvp in options.ConnectionStrings)
    {
        SqlConnectionStringBuilder builder = new(kvp.Value);
        if (!builder.MultipleActiveResultSets)
        {
            builder.MultipleActiveResultSets = true;
            logger?.LogInformation(
                "[MARS] ConnectionString '{Key}'에 MultipleActiveResultSets=True 자동 주입",
                kvp.Key);
        }
        corrected[kvp.Key] = builder.ConnectionString;
    }
    options.ConnectionStrings = corrected;
}
```

- [ ] **Step 2: 빌드 검증 + 커밋**

```bash
git add Lib.Db/Lib.Db/Extensions/LibDbServiceCollectionExtensions.cs
git commit -m "feat: MarsPolicy.ForceEnable — ConnectionString MARS 자동 주입 (T6-3)"
```

---

### Task 6.3: ValidateMarsEnabled 정책 분기 + SchemaRepository 폴백 [T6-4, T6-5]

**Files:**
- Modify: `Lib.Db/Lib.Db/Execution/Executors/SqlDbExecutor.cs:918-946`
- Modify: `Lib.Db/Lib.Db/Infrastructure/Infrastructure.cs:370-400`

- [ ] **Step 1: ValidateMarsEnabled에 MarsPolicy 주입**

`SqlDbExecutor` 생성자에서 `LibDbOptions`를 받아 `_marsPolicy` 필드로 저장. `ValidateMarsEnabled`에서 정책 분기:

```csharp
private void ValidateMarsEnabled(System.Data.Common.DbConnection conn)
{
    if (_marsPolicy == MarsPolicy.ForceEnable)
        return; // 이미 보정됨

    if (_marsPolicy == MarsPolicy.Disabled)
    {
        ThrowMarsDisabled();
        return;
    }

    // Auto 모드: 기존 검증 로직
    // ... (기존 ValidateMarsEnabled 로직)
}

private static void ThrowMarsDisabled()
{
    throw new InvalidOperationException(
        "MarsPolicy가 Disabled로 설정되어 QueryMultipleAsync를 사용할 수 없습니다. " +
        "LibDbOptions.Mars를 Auto 또는 ForceEnable로 변경하세요.");
}
```

- [ ] **Step 2: SqlSchemaRepository.GetSpMetadataAsync — Disabled 폴백**

`MarsPolicy.Disabled`일 때 다중 ResultSet 대신 별도 쿼리 2회:

```csharp
if (marsPolicy == MarsPolicy.Disabled)
{
    // 버전 조회
    SpSchema? version = await GetSpVersionAsync(name, instanceHash, ct);
    // 파라미터 조회
    List<SpParam> parameters = await GetSpParametersAsync(name, instanceHash, ct);
    return new SpMetadata(version, parameters);
}
else
{
    // 기존 MARS 기반 다중 ResultSet 로직
}
```

- [ ] **Step 3: 빌드 검증 + 커밋**

```bash
git add Lib.Db/Lib.Db/Execution/Executors/SqlDbExecutor.cs Lib.Db/Lib.Db/Infrastructure/Infrastructure.cs
git commit -m "feat: ValidateMarsEnabled 정책 분기, SchemaRepository Disabled 폴백 (T6-4,T6-5)"
```

---

### Task 6.4: Validator + 문서 업데이트 [T6-6, T6-7]

**Files:**
- Modify: `Lib.Db/Lib.Db/Configuration/LibDbOptionsValidator.cs`
- Modify: `Lib.Db/docs/04_operations.md`

- [ ] **Step 1: LibDbOptionsValidator에 MARS 검증 추가**

```csharp
// ForceEnable 시 ConnectionString 파싱 가능 여부 검증
if (options.Mars == MarsPolicy.ForceEnable)
{
    foreach (KeyValuePair<string, string> kvp in options.ConnectionStrings)
    {
        try { _ = new SqlConnectionStringBuilder(kvp.Value); }
        catch (Exception ex)
        {
            errors.Add($"ConnectionString '{kvp.Key}' 파싱 실패 (MARS 자동 주입 불가): {ex.Message}");
        }
    }
}
```

- [ ] **Step 2: 04_operations.md 프로덕션 체크리스트에 MARS 정책 가이드 추가**

- [ ] **Step 3: 빌드 검증 + 전체 테스트 + 커밋**

Run: `dotnet build Lib.Db/Lib.Db.slnx && dotnet test Lib.Db/Lib.Db.slnx`

```bash
git add Lib.Db/Lib.Db/Configuration/LibDbOptionsValidator.cs Lib.Db/docs/04_operations.md
git commit -m "feat: MARS Validator 검증, 운영 문서 업데이트 (T6-6,T6-7)"
```

---

## 최종 검증

### Task FINAL: 전체 빌드 + 전체 테스트 + 패키지 생성

- [ ] **Step 1: 전체 빌드**

Run: `dotnet build Lib.Db/Lib.Db.slnx -c Release`
Expected: 빌드 성공, 경고 최소화

- [ ] **Step 2: 전체 단위 테스트**

Run: `dotnet test Lib.Db/Lib.Db.slnx --filter "FullyQualifiedName~Unit or FullyQualifiedName~Architecture or FullyQualifiedName~Aot or FullyQualifiedName~Binding or FullyQualifiedName~Caching or FullyQualifiedName~Diagnostics or FullyQualifiedName~Executors"`
Expected: 모든 테스트 PASS

- [ ] **Step 3: NuGet 패키지 생성**

Run: `dotnet pack Lib.Db/Lib.Db/Lib.Db.csproj -c Release`
Expected: .nupkg + .snupkg 생성

- [ ] **Step 4: 버전 업데이트**

`Lib.Db.csproj`의 `<Version>` 태그를 `2.2.0`으로 변경.

- [ ] **Step 5: 최종 커밋**

```bash
git add -A
git commit -m "release: Lib.Db v2.2.0 — 최적화, 버그 수정, MARS 정책, 한글 주석 완비"
```
